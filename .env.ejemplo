# Copiar este archivo a .env y completar con tus propios valores.
# Nunca versionar el .env real (ya esta en .gitignore).

# Free tier de la API de Gemini (https://aistudio.google.com/apikey).
GEMINI_API_KEY=
